# Root-level env, read by prisma.config.ts for CLI commands run from the
# workspace root (prisma validate / migrate deploy / generate / studio).
# Each app also has its own .env.example — see apps/dashboard and apps/landing.

# REQUIRED — PostgreSQL connection string used by Prisma migrations/CLI.
DATABASE_URL="postgresql://user:password@host:5432/eatlink"
